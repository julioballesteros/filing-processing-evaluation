"""Allow the CLI to run with ``python -m filing_processing_evaluation``."""

from filing_processing_evaluation.cli import app  # pragma: no cover

if __name__ == "__main__":  # pragma: no cover
    app()
